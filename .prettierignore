# Generated output
dist/
build/
.svelte-kit/
.output/
coverage/
playwright-report/
test-results/
# Generated fee-schedule lookup tables (formatted by scripts/build-fee-schedules.mjs;
# the reproducibility check in CI is the real gate, not Prettier).
apps/frontend/src/lib/data/goae.json
apps/frontend/src/lib/data/goz.json
apps/frontend/src/lib/data/got.json

# Dependencies & lockfile
node_modules/
pnpm-lock.yaml

# Generated by drizzle-kit (SQL migrations + its internal snapshot metadata) —
# regenerated via `pnpm --filter backend db:generate`, not hand-formatted.
apps/backend/src/db/migrations/

# Source data (legal-text XML exports) and runtime data
data/

# On-device OCR assets fetched/copied at build time (git-ignored vendor binaries
# + their emscripten glue): PP-OCRv5 models and the ONNX Runtime WASM. Populated
# by `pnpm ocr:models` / `scripts/copy-ort-wasm.mjs`, never hand-formatted.
apps/frontend/static/models/ocr/*.onnx
apps/frontend/static/models/ocr/dict.txt
apps/frontend/static/models/ort/*.mjs
apps/frontend/static/models/ort/*.wasm

# Authored long-form German documentation (single source of truth — kept
# hand-formatted, not reflowed by Prettier).
docs/
CLAUDE.md
